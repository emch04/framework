{
  "name": "@astratra/native",
  "version": "0.1.0",
  "description": "Mobile plumbing without the mobile runtime: secure token session with an injected keystore, a biometric gate, native push registration and foreground watching, and a checkout round-trip that survives a missing native module. Adapters injected, so it tests in plain Node.",
  "engines": {
    "node": ">=20"
  },
  "main": "src/index.js",
  "types": "src/index.d.ts",
  "type": "commonjs",
  "files": [
    "src"
  ],
  "scripts": {
    "test": "jest"
  },
  "jest": {
    "testMatch": [
      "**/__tests__/**/*.test.js"
    ]
  },
  "keywords": [
    "astratra",
    "mobile",
    "react-native",
    "expo",
    "session",
    "biometric",
    "push"
  ],
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "jest": "30.4.2"
  },
  "dependencies": {
    "@astratra/client": "^0.3.0"
  }
}
